ensemble Counter:
    motif tune(self, start):
        self._n = start

    motif bump(self):
        self._n = self._n + 1

    motif value(self):
        resolve self._n

c = Counter(10)
loop i in scale(3):
    c.bump()

play(c.value())
