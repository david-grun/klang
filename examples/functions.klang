motif make_adder(n):
    motif add(x):
        resolve x + n
    resolve add

motif factorial(n):
    when n <= 1:
        resolve 1
    resolve n * factorial(n - 1)

add_five = make_adder(5)
play(add_five(10))
play(factorial(5))
